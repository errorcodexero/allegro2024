Item                                                                    Status
------------------------------------------------------------------------------
Manipulator: Check SparkFlex/Vortex startup
Tilt: Check angles while disabled
UpDown: Check angles while disabled

Tilt: Tune (kv, kp, maxvel, maxaccel)
      - experiment with jerk
      - check position to target
      - check position following
      - check velocity following
      - optimize maxvel & maxaccel

UpDown: Tune (kv, kp, maxvel, maxaccel)
      - check position to target
      - check position following
      - check velocity following
      - optimize maxvel & maxaccel

Shooter 1/2: Tune (kv, kp)
      - check actual velocity vs target

Feeder: power only, no tuning

Arm: Tune (kv, kp, maxvel, maxaccel)
      - check position to target
      - check position following
      - check velocity following
      - optimize maxvel & maxaccel

Elevator: Tune (kv, kp, maxvel, maxaccel)
      - check position to target
      - check position following
      - check velocity following
      - optimize maxvel & maxaccel

Climber: Tune (kv, kp, maxvel, maxaccel)
      - check position to target
      - check position following
      - check velocity following
      - optimize maxvel & maxaccel

Manipulator: power only, no tuning

Start working climb sequence w/OI
